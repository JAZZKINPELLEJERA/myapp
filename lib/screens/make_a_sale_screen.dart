
import 'package:flutter/material.dart';

class MakeASaleScreen extends StatelessWidget {
  const MakeASaleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Make a Sale Screen'),
    );
  }
}
